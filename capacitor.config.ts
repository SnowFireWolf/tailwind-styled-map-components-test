import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sfirew.dipper',
  appName: 'dipper-frontend-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
